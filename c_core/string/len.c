// strlen(): Computes the length of the string (excluding the null character).

#include <stdio.h>
#include <string.h>

int main() {
    char name[] = "brijesh";
    
    // int len = strlen(name);
    
    printf("%d", len);

    return 0;
}